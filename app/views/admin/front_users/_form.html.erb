<%= form_for [:admin, @front_user] do |f| %>
  <%= render "/shared/form_errors", :object => f.object %>
  <fieldset>
    <legend><%= "FrontUser attributes" %></legend>
    <hr>

    <div class="row mb-3">
      <%= f.label :name, class: "col-sm-2 col-form-label text-end"%>
      <div class="col-sm-10">
        <%= f.text_field :name, class: "form-control", placeholder: t("activerecord.attributes.front_user.name") %>
        <div class="form-text">Give a name to your front_user.</div>
      </div>
    </div>

    <div class="row mb-3">
      <%= f.label :age, class: "col-sm-2 col-form-label text-end"%>
      <div class="col-sm-10">
        <%= f.number_field :age, class: "form-control", placeholder: t("activerecord.attributes.front_user.age") %>
      </div>
    </div>

    <div class="row mb-3">
      <%= f.label :country, class: "col-sm-2 col-form-label text-end"%>
      <div class="col-sm-10">
        <%= f.text_field :country, class: "form-control", placeholder: t("activerecord.attributes.front_user.country") %>
      </div>
    </div>

    <div class="row mb-3">
      <%= f.label :wannabe, class: "col-sm-2 col-form-label text-end"%>
      <div class="col-sm-10">
        <%= f.text_field :wannabe, class: "form-control", placeholder: t("activerecord.attributes.front_user.wannabe") %>
      </div>
    </div>

    <div class="row mb-3">
      <%= f.label :email, class: "col-sm-2 col-form-label text-end"%>
      <div class="col-sm-10">
        <%= f.text_field :email, class: "form-control", placeholder: t("activerecord.attributes.front_user.email") %>
        <div class="form-text">Give a email to your front_user.</div>
      </div>
    </div>

    <div class="row mb-3">
      <%= f.label :password, class: "col-sm-2 col-form-label text-end"%>
      <div class="col-sm-10">
        <%= f.password_field :password, class: "form-control", placeholder: t("activerecord.attributes.front_user.password") %>
        <div class="form-text">Give a password to your front_user.</div>
      </div>
    </div>

    <div class="row">
      <div class="offset-sm-2 col">
        <%= f.submit class: "btn btn-primary" %>
      </div>
  </fieldset>
<% end %>
